辗转相除法

 我们要求 $gcd(a,b)\quad (b\geq a)$ , 我们可以采用如下方法：
 1. 将 $b$ 表示为 
 $$b=c b+d$$
 2. 在取出，$b$ 和 $d$ 进行相似的操作，得到 $b_{1}, d_{1}$ 重复操作直到
 $$b_{n-1}=c_{n}d_{n-1}$$此时，$gcd(a,b)=d_{n-1}$ 

$Bezout 定理$ 

设 $a,b\in \mathbb{N}$ , 则存在 $k,l \in \mathbb{Z}$ , 使得 
$$gcd(a,b)=ka+lb$$
由辗转相除法逆用可以得到：
$$d_{n-1}=b_{n-2}-c_{n-1}d_{n-2}$$持续该操作可以得到
$$d_{n-1} = b_{n-2} - c_{n-1} b_{n-3} + c_{n-1} c_{n-2} b_{n-4} - \cdots + (-1)^{n-1} c_{n-1} c_{n-2} \cdots c_2 b_0 + (-1)^n c_{n-1} c_{n-2} \cdots c_1 d_0.$$
更紧凑的求和形式为：
$$
d_{n-1} = \sum_{k=0}^{n-2} \left[ (-1)^k \left( \prod_{j=0}^{k-1} c_{n-1-j} \right) b_{n-2-k} \right] + (-1)^{n-1} \left( \prod_{j=1}^{n-1} c_j \right) d_0,
$$
其中当 $k=0$ 时，空乘积 $\displaystyle \prod_{j=0}^{-1} c_{n-1-j}$ 约定为 $1$。

PS：这里会用就可以，不用那么复杂。但是非常重要

探究： 假设 $a,b \in \mathbb{N}$ 
设 $S=\{ka+lb : k,l \in \mathbb{Z}\}$ 
由 $Bezout \ 定理$ 可知 $gcd(a,b)\in S$
设 $gcd(a,b)=k_{0}a+l_{0}b$ , 则对任意 $m \in \mathbb{Z}$ 
$$dm=gcd(a,b)m=k_{0}ma+l_{0}mb\in S$$即 $$\{\cdots,-2d,-d,0,d,2d,\cdots \}\subset S$$

proof.  $\{\cdots,-2d,-d,0,d,2d,\cdots \}= S$ 
设有一个数 $c=ka+lb$ 
设 $a=da'$ , $b=db'$ ,那么 $c$ 可以表示成 
$$c=ka'd+lb'd=(ka'+lb')d \in S$$

we marked is as :
Def. 设 $a\in \mathbb{N}$ ,定义 $a\mathbb{Z}=\{ak: k \in \mathbb{Z}\}=\{\cdots,-2a,-a,0,a,2a,\cdots \}$ 
因此 ，若 $a,b \in \mathbb{N}$ 则 
$$\{ka+lb:k,l \in \mathbb{Z}\}=gcd(a,b)\mathbb{Z}$$
Property. 设 $a,b,e \in \mathbb{N}$ 则
$$e \mid a\ ，\ e \mid b$$
则 $e\mid gcd(a,b)$

proof. 由 $Bezout 定理$ 存在 $k,l \in \mathbb{Z}$ .
$$gcd(a,b)=ka+lb $$由 $e \mid a$ , $e \mid b$ 有 $e\mid gcd(a,b)$

Ⅰ中有一个未证明的性质[[Ⅰ整除#75#78]]
性质

1. 有 $gcd(ka,kb)=k\ gcd(a,b)$ 
2.  有 $lcm(ka,kb)= k\ lcm(a,b)$ 

proof. 1 
$\{m(ka)+n(kb):m,n \in \mathbb{Z}\}=\{(mk)a+(nk)b:m,n \in \mathbb{Z}\}=\{k(ma+nb):m.n \in \mathbb{Z}\}$
由上述一些命题 $\{ma+nb:m,n\in \mathbb{Z}\}=gcd(a,b)\mathbb{Z}$ 
因此 $S=k\ \cdot\ gcd(a,b)$

proof. 2 
要证：$gcd(ka,kb)=k\ \cdot\ gcd(a,b)$ 则
1.  $k\ \cdot\ \gcd(a,b) \mid ka$
   $k\ \cdot\ \gcd(a,b) \mid ka$ 
   因此 $k\ \cdot\ \gcd(a,b) \mid gcd(ka,kb)$ 
2. 要证： $\frac{gcd(ka,kb)}{k} \mid gcd(a,b)$
   由于 $k \mid ka$ , $k \mid kb$ 因此 $k \mid gcd(ka,kb)$ 
   $gcd(ka,kb) \mid ka$ 设 $ka=gcd(ka,kb)c$ 
   则 $\frac{gcd(ka,kb)}{k}c=a$
   因此 $\frac{gcd(ka,kb)}{k} \mid a$ , $\frac{gcd(ka,kb)}{k} \mid b$ .因此得证

性质
设 $a,b,c \in \mathbb{N}$ 
若 $a \mid bc$ , 且 $(a,b)=1$ 
则 $a \mid c$  
proof. 由 $Bezont\ 定理$ 设 $ka+lb=1\ (k,l \in \mathbb{Z})$
由 $a \mid bc$ 设 $bc=ad$
target : proof $a \mid c$ 
$kac+lbc=c$  与 $kac+lad=c$ 于是我们得到 
$$a \mid (kac+lad)=c$$

推论：设 $a,b \in \mathbb{N}$ , $c\in \mathbb{Z}$
设 $a \mid bc$ .
则 $\frac{a}{gcd(a,b)}\mid c$
proof.  $gcd(a,b)$ 同时整除 $a$ 和 $b$ 
由 $a \mid bc$ 可知 $\frac{a}{gcd(a,b)} \mid \frac{b}{gcd(a,b)}c$
我们前面证明了 $\frac{a}{gcd(a,b)}$ ,  $\frac{b}{gcd(a,b)}$ 互素
由上述性质可知，$\frac{a}{gcd(a,b)} \mid c$ 

最小公倍数

性质 ，设 $a,b \in \mathbb{N}$ 
若 $gcd(a.b)=1$ , 则 $lcm(a,b)=ab$  
