### LECTURE 1. BASIC PROPERTIES FOR DIRICHLET L-FUNCTION

#### 1. Euler's Product

**Def 1 (Dirichlet L-function)**
Let $\chi$ be a Dirichlet character. We define the L-function:
$$L(s, \chi) := \sum_{n=1}^{\infty} \frac{\chi(n)}{n^s}, \quad s > 1$$

**Thm 1 (Dirichlet-Euler)**
If $s > 1$, then:
$$\sum_{n=1}^{\infty} \frac{\chi(n)}{n^s} = \prod_{p} \frac{1}{1 - \chi(p)p^{-s}}$$

**Prop 2 (Convergence of infinite product)**
If $\sum a_n < \infty$ and $a_n \neq 0$, then:
$$0 \neq \prod_{n=1}^{\infty} (1 - a_n)^{-1} < \infty$$

---

#### 2. Basic Properties of L-Function

**Prop 3 (Trivial character $\chi_0$)**
If $\chi_0$ is a trivial character modulo $q$, then:
$$L(s, \chi_0) = \prod_{p|q} (1 - p^{-s}) \zeta(s)$$
In particular, $L(s, \chi_0) \to \infty$ as $s \downarrow 1$.

**Prop 4 (Behavior of $L(s, \chi)$ for nontrivial $\chi$)**
Suppose $\chi$ is nontrivial. Then:
1.  $L(s, \chi) < \infty$ for $s > 0$.
2.  $L(s, \chi) \in C^{\infty}(0, \infty)$.
3.  $\exists c > 0$ such that $L(s, \chi) = 1 + O(e^{-cs})$ as $s \to \infty$.

**Lemma (Convolution property)**
Assume $\chi$ is a nontrivial character, then $\sum_{n=1}^{q} \chi(n) = 0$.

---

#### 3. NON-VANISHING OF L-FUNCTION (COMPLEX CASE)

**Lemma 2**
If $L(1, \chi) = 0$, then:
$$\log |L(s, \chi)| \ll 1 \quad \text{as} \quad s \downarrow 1$$

**Lemma 3**
1.  If $L(1, \chi) = 0$, then $L(1, \overline{\chi}) = 0$.
2.  If $\chi$ is nontrivial and $L(1, \chi) = 0$, then $|L(s, \chi)| \leq C|s-1|$ for $s \leq 2$.
3.  For trivial $\chi_0$, we have $|L(s, \chi_0)| \geq c|s-1|$.

**Theorem (Non-vanishing at s=1)**
If $\chi$ is nontrivial, then $L(1, \chi) \neq 0$.

**Proof Sketch:**
By contradiction. Assume $L(1, \chi) = 0$.
1.  By Lemma 3, both $L(s, \chi)$ and $L(s, \overline{\chi})$ contribute terms like $O(|s-1|)$.
2.  However, only the trivial character $\chi_0$ contributes a term like $\frac{1}{s-1}$.
3.  This leads to a contradiction regarding the growth rate of the product $\prod L(s, \chi)$, implying our assumption is false.
$$ \therefore L(1, \chi) \neq 0 $$
